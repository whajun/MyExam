#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(void)
{
	int num = 0, max = 0, index = 0;
	for (int i = 1; i <= 9; i++) {
		scanf("%d", &num);
		if (num > max) {
			max = num;
			index = i;
		}
	}
	printf("%d\n%d\n", max, index);
	return 0;
}
